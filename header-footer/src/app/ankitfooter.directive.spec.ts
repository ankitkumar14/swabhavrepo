import { AnkitfooterDirective } from './ankitfooter.directive';

describe('AnkitfooterDirective', () => {
  it('should create an instance', () => {
    const directive = new AnkitfooterDirective();
    expect(directive).toBeTruthy();
  });
});

import { AnkitheaderDirective } from './ankitheader.directive';

describe('AnkitheaderDirective', () => {
  it('should create an instance', () => {
    const directive = new AnkitheaderDirective();
    expect(directive).toBeTruthy();
  });
});
